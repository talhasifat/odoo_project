<odoo>
    <!-- Search View -->
    <record id="view_school_student_search" model="ir.ui.view">
        <field name="name">school.student.search</field>
        <field name="model">school.student</field>
        <field name="arch" type="xml">
            <search string="Search Students">
                <field name="class_id"/>
                <field name="section_id"/>
                <field name="subject_id"/>
                <field name="roll_number" string="Roll Number"/>
            </search>
        </field>
    </record>

    <!-- Form View -->
    <record id="view_school_student_form" model="ir.ui.view">
        <field name="name">school.student.form</field>
        <field name="model">school.student</field>
        <field name="arch" type="xml">
            <form string="Student">
                <sheet>
                    <group col="2">
                        <field name="image"/>
                        <group>
                            <field name="name"/>
                            <field name="roll_number"/>
                            <field name="age"/>
                            <field name="gender"/>
                            <field name="date_of_birth"/>
                            <field name="email"/>
                            <field name="phone"/>
                            <field name="class_id"/>
                            <field name="section_id"/>
                            <field name="subject_id"/> <!-- ✅ Subject added here -->
                            <field name="institution_id"/>
                            <field name="address"/>
                        </group>
                    </group>
                </sheet>
            </form>
        </field>
    </record>

    <!-- List View -->
    <record id="view_school_student_list" model="ir.ui.view">
        <field name="name">school.student.list</field>
        <field name="model">school.student</field>
        <field name="arch" type="xml">
            <list string="Students">
                <field name="image" widget="image" options="{'widget_size': 40}"/>
                <field name="name"/>
                <field name="roll_number"/>
                <field name="age"/>
                <field name="gender"/>
                <field name="email"/>
                <field name="class_id"/>
                <field name="section_id"/>
                <field name="subject_id"/> <!-- ✅ Subject added here -->
            </list>
        </field>
    </record>

    <!-- Action -->
    <record id="action_school_student" model="ir.actions.act_window">
        <field name="name">Students</field>
        <field name="res_model">school.student</field>
        <field name="view_mode">list,form</field>
    </record>
</odoo>
