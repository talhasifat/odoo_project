<odoo>

    <!-- ===== School Class ===== -->
    <record id="view_school_class_form" model="ir.ui.view">
        <field name="name">school.class.form</field>
        <field name="model">school.class</field>
        <field name="arch" type="xml">
            <form string="Class">
                <sheet><group><field name="name"/></group></sheet>
            </form>
        </field>
    </record>

    <record id="view_school_class_list" model="ir.ui.view">
        <field name="name">school.class.list</field>
        <field name="model">school.class</field>
        <field name="arch" type="xml">
            <list string="Classes">
                <field name="name"/>
            </list>
        </field>
    </record>

    <record id="action_school_class" model="ir.actions.act_window">
        <field name="name">Classes</field>
        <field name="res_model">school.class</field>
        <field name="view_mode">list,form</field>
    </record>

    <!-- ===== School Section ===== -->
    <record id="view_school_section_form" model="ir.ui.view">
        <field name="name">school.section.form</field>
        <field name="model">school.section</field>
        <field name="arch" type="xml">
            <form string="Section">
                <sheet><group><field name="name"/></group></sheet>
            </form>
        </field>
    </record>

    <record id="view_school_section_list" model="ir.ui.view">
        <field name="name">school.section.list</field>
        <field name="model">school.section</field>
        <field name="arch" type="xml">
            <list string="Sections">
                <field name="name"/>
            </list>
        </field>
    </record>

    <record id="action_school_section" model="ir.actions.act_window">
        <field name="name">Sections</field>
        <field name="res_model">school.section</field>
        <field name="view_mode">list,form</field>
    </record>

    <!-- ===== School Institution ===== -->
    <record id="view_school_institution_form" model="ir.ui.view">
        <field name="name">school.institution.form</field>
        <field name="model">school.institution</field>
        <field name="arch" type="xml">
            <form string="Institution">
                <sheet><group><field name="name"/></group></sheet>
            </form>
        </field>
    </record>

    <record id="view_school_institution_list" model="ir.ui.view">
        <field name="name">school.institution.list</field>
        <field name="model">school.institution</field>
        <field name="arch" type="xml">
            <list string="Institutions">
                <field name="name"/>
            </list>
        </field>
    </record>

    <record id="action_school_institution" model="ir.actions.act_window">
        <field name="name">Institutions</field>
        <field name="res_model">school.institution</field>
        <field name="view_mode">list,form</field>
    </record>

    <!-- ===== School Subject ===== -->
    <record id="view_school_subject_form" model="ir.ui.view">
        <field name="name">school.subject.form</field>
        <field name="model">school.subject</field>
        <field name="arch" type="xml">
            <form string="Subject">
                <sheet><group><field name="name"/></group></sheet>
            </form>
        </field>
    </record>

    <record id="view_school_subject_list" model="ir.ui.view">
        <field name="name">school.subject.list</field>
        <field name="model">school.subject</field>
        <field name="arch" type="xml">
            <list string="Subjects">
                <field name="name"/>
            </list>
        </field>
    </record>

    <record id="action_school_subject" model="ir.actions.act_window">
        <field name="name">Subjects</field>
        <field name="res_model">school.subject</field>
        <field name="view_mode">list,form</field>
    </record>

    <!-- ===== Grade Configuration ===== -->
    <record id="view_school_grade_form" model="ir.ui.view">
        <field name="name">school.grade.form</field>
        <field name="model">school.grade</field>
        <field name="arch" type="xml">
            <form string="Grade">
                <sheet>
                    <group>
                        <field name="name"/>
                        <field name="mark_from"/>
                        <field name="mark_to"/>
                    </group>
                </sheet>
            </form>
        </field>
    </record>

    <record id="view_school_grade_list" model="ir.ui.view">
        <field name="name">school.grade.list</field>
        <field name="model">school.grade</field>
        <field name="arch" type="xml">
            <list string="Grades">
                <field name="name"/>
                <field name="mark_from"/>
                <field name="mark_to"/>
            </list>
        </field>
    </record>

    <record id="action_school_grade" model="ir.actions.act_window">
        <field name="name">Grades</field>
        <field name="res_model">school.grade</field>
        <field name="view_mode">list,form</field>
    </record>


    <menuitem id="menu_school_grade_action"
              name="Grades"
              parent="menu_school_configuration"
              action="student_information.action_school_grade"
              sequence="6"/>

</odoo>